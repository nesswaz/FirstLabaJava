# Вариант 7
# Задание 1
## Задача 1
### Текст задачи 
Дробная часть. 
Дана сигнатура метода: public double fraction (double x); Необходимо реализовать метод таким образом, чтобы он возвращал только дробную часть числа х. Подсказка: вещественное число может быть преобразовано к целому путем отбрасывания дробной части. 

Пример: 
x=5,25 
результат: 0,25
### Алгоритм решения
1. Получить число "x" вещественного типа.
2. Создать переменную "a" целочисленного типа, присвоить значение "x", с помощью приведения типов (int), чтобы у нас получилась целая часть числа (дробная часть откинется). 
3. Вычесть из исходного числа целую часть.
## Задача 4 
### Текст задачи 
Есть ли позитив. 
Дана сигнатура метода: public bool isPositive (int x); Необходимо реализовать метод таким образом, чтобы он принимал число x и возвращал true, если оно положительное. 

Пример 1: 
x=3 
результат: true 

Пример 2:
x=-5 
результат: false
### Алгоритм решения
1. Получить число "x".
2. Сравнить число с нулем (>0). 
3. Если число положительноe, вернуть true, иначе вывести false.
## Задача 5
### Текст задачи
Двузначное. 
Дана сигнатура метода: public bool is2Digits (int x); Необходимо реализовать метод таким образом, чтобы он принимал число x и возвращал true, если оно двузначное. 

Пример 1:
x=32 
результат: true 

Пример 2: 
x=516 
результат: false
### Алгоритм решения
1. Получить число "x".
2. Проверить находится ли оно между 10 и 99, -10 и -99.
3. Если число подходит под условия, вернуть true. Иначе false.
## Задача 8
### Текст задачи
Делитель. 
Дана сигнатура метода: public bool isDivisor (int a, int b); Необходимо реализовать метод таким образом, чтобы он возвращал true, если любое из принятых чисел делит другое нацело. 

Пример 1: 
a=3 b=6 
результат: true 

Пример 2: 
a=2 b=15 
результат: false
### Алгоритм решения 
1.  Получить два целых числа a и b.
2.  Проверить, что числа ненулевые (для избежания ошибки деления на ноль). 
3.  Проверить, что первое число делится на второе и еще проверить, что второе число делится на первое. 
4. Если делится нацело, вывести true, иначе - false.
## Задача 9
### Текст задачи
Равенство. 
Дана сигнатура метода: public bool isEqual(int a, int b, int c); Необходимо реализовать метод таким образом, чтобы он возвращал true, если все три полученных методом числа равны 

Пример 1: 
a=3 b=3 с=3 
результат: true 

Пример 2: 
a=2 b=15 с=2 
результат: false
### Алгоритм решения
1. Получить три целых числа - a, b, c.
2. Сравнить первое число со вторым и первое число с третьим.
3. Если выполняется, вывести true, иначе - false.
# Задание 2
## Задача 3
### Текст задачи
Тридцать пять. 
Дана сигнатура метода: public bool is35 (int x); Необходимо реализовать метод таким образом, чтобы он возвращал true, если число x делится нацело на 3 или 5. При этом, если оно делится и на 3, и на 5, то вернуть надо false. Подсказка: оператор % позволяет получить остаток от деления. 

Пример 1: 
x=5 
результат: true 

Пример 2: 
x=8
результат: false 

Пример 3:
x=15 
результат: false
### Алгоритм решения
1. Получить целое число x.
2. Проверить число, делится ли оно на 3 и на 5. Если делится, то вывести false.
3. Иначе, проверить, если число делится на 3 или на 5, то вывести true.
4. Иначе, вывести false.
## Задача 5
### Текст задачи
Тройной максимум.
Дана сигнатура метода: public int max3 (int x, int y, int z); Необходимо реализовать метод таким образом, чтобы он возвращал максимальное из трех полученных методом чисел. Подсказка: идеальное решение включает всего две инструкции if и не содержит вложенных if. 

Пример 1: 
x=5 y=7 z=7 
результат: 7 

Пример 2: 
x=8 y=-1 z=4 
результат: 8
### Алгоритм решения
1. Получить три целых числа - x, y, z.
2. Создать переменную max (максимум), присвоить к ней число x.
3. Сравнить, если второе число больше max, то присвоить к максимуму второе число.
4. Сравнить, если третье число больше max, то присвоить к максимуму третье число.
5. Вывести значение переменной max.
## Задача 7
### Текст задачи 
Двойная сумма.
Дана сигнатура метода: public int sum2 (int x, int y); Необходимо реализовать метод таким образом, чтобы он возвращал сумму чисел x и y. Однако, если сумма попадает в диапазон от 10 до 19, то надо вернуть число 20. 

Пример 1:
x=5 y=7 
результат: 20 

Пример 2: 
x=8 y=-1 
результат: 7
### Алгоритм решения
1. Получить два целых числа - x, y.
2. Создать переменную целого типа - sum (сумму), сложить два числа.
3. Сравнить, если сумма входит в диапазон от 10 до 19, вывести 20. Иначе - вывести просто сумму.
## Задача 8
### Текст задачи
Возраст. 
Дана сигнатура метода: public String age (int x); Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой сначала будет число х, а затем одно из слов: год года лет Слово “год” добавляется, если число х заканчивается на 1, кроме числа 11. Слово “года” добавляется, если число х заканчивается на 2, 3 или 4, кроме чисел 12, 13, 14. Слово “лет” добавляется во всех остальных случаях. 
Подсказка: оператор % позволяет получить остаток от деления. 

Пример 1: 
x=5 
результат: “5 лет” 

Пример 2: 
x=31 
результат: “31 год” 

Пример 3: 
x=44 
результат: “44 года”
### Алгоритм решения
1. Получить целое число x.
2. Сначала поделить число на 100 по модулю, сравнить входит ли этот остаток в диапазон от 11 до 14 включительно. Если входит, то вернуть значение с подписью " лет".
3.  Поделить число на 10 по модулю, сравнить является ли остаток единицей, если да, то вернуть значение с подписью " год".
4. Поделить число на 10 по модулю, сравнить входит ли остаток в диапазон от 2 до 4 включительно. Если входит, то вернуть значение с подписью " года". 
5. В остальных всех случаях вернуть значение с подписью " лет".
## Задача 10
### Текст задачи
Вывод дней недели. 
Дана сигнатура метода: public void printDays (String x); В качестве параметра метод принимает строку, в которой записано название дня недели. Необходимо реализовать метод таким образом, чтобы он выводил на экран название переданного в него дня и всех последующих до конца недели дней. Если в качестве строки передан не день, то выводится текст “это не день недели”. Первый день понедельник, последний – воскресенье. Вместо if в данной задаче используйте switch. 

Пример 1: 
x=”четверг” 
результат: 
четверг 
пятница 
суббота 
воскресенье 

Пример 2: x=”чг” 
результат: 
это не день недели
### Алгоритм решения
1. Получить строковое значение x.
2. Привести строку "x" - к нижнему регистру, с помощью метода toLowerCase().
3. Создать switch с проверкой переменной x, у каждого case свое строковое значение - дни недели: понедельник, вторник, ... , воскресенье.
4. В каждом case написать вывод названия дня недели, которое соответствует текущему значению.
5. Только в последнем case со значением "воскресенье" поставить break, чтобы программа остановилась. 
6. В итоге будет выводится переданный день и последующие дни до конца недели.
7. Написать default, в случае ошибочного/неверного ввода.
# Задание 3
## Задача 2
### Текст задачи
Числа наоборот. 
Дана сигнатура метода: public String reverseListNums (int x); Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой будут записаны все числа от x до 0 (включительно). 

Пример: 
x=5 
результат: “5 4 3 2 1 0”
### Алгоритм решения
1. Получить целое число x.
2. Создать пустую строку.
3. Написать цикл с числа "x", на каждом шаге уменьшать число на единицу, пока не будет достигнут 0.
4. В пустую строку записывать значения.
5. Вернуть итоговый результат.
## Задача 4
### Текст задачи
Степень числа. 
Дана сигнатура метода: public int pow (int x, int y); Необходимо реализовать метод таким образом, чтобы он возвращал результат возведения x в степень y. Подсказка: для получения степени необходимо умножить единицу на число x, и сделать это y раз, т.е. два в третьей степени это 
1 * 2 * 2 * 2 

Пример: 
x=2 y=5 
результат: 32
### Алгоритм решения
1. Получить целые числа x и y.
2. Создать переменную целого типа с единицей. 
3. Написать цикл y раз, на каждом шаге умножать num на x.
4. Вывести результат.
## Задача 6
### Текст задачи
Одинаковость. 
Дана сигнатура метода: public bool equalNum (int x); Необходимо реализовать метод таким образом, чтобы он возвращал true, если все знаки числа одинаковы, и false в ином случае. 
Подсказки: intx=123%10; // х будет иметь значение 3 intу=123/10; // у будет иметь значение 12

Пример 1: 
x=1111 
результат: true 

Пример 2: 
x=1211 
результат: false
### Алгоритм решения
1. Получить целое число "x".
2. Создать переменную целого типа, для поиска последней цифры числа. Разделить число на 10. 
3. Написать цикл, если число больше 0, начать сравнивать каждую последнюю цифру числа, с помощью деления по модулю. Если не равно, вернуть значение false. 
4. Иначе вернуть true.
## Задача 9
### Текст задачи
Правый треугольник. 
Дана сигнатура метода: public void rightTriangle (int x); Необходимо реализовать метод таким образом, чтобы он выводил на экран треугольник из символов  ‘ * ’  у которого х символов в высоту, а количество символов в ряду совпадает с номером строки, при этом треугольник выровнен по правому краю. Подсказка: перед символами ‘ * ’ следует выводить необходимое количество пробелов. 
### Алгоритм решения
1. Получить целое число - размера треугольника "x".
2. Создать внешний цикл от 1 до "x" - переборы строк.
3. Создать внутренний цикл для вывода пробелов в количестве x - i.
4. Создать второй внутренний цикл для вывода символов " * ".
5. После каждой выведенной строки поставить переход на следующую строку.
## Задача 10
### Текст задачи
Угадайка. 
Дана сигнатура метода: public void guessGame() Необходимо реализовать метод таким образом, чтобы он генерировал случайное число от 0 до 9, далее считывал с консоли введенное пользователем число и выводил, угадал ли пользователь то, что было загадано, или нет. Метод запускается до тех пор, пока пользователь не угадает число. После этого выведите на экран количество попыток, которое потребовалось пользователю, чтобы угадать число. 
Пример: 
Введите число от 0 до 9: 5 Вы не угадали, введите число от 0 до 9: 9 Вы угадали! Вы отгадали число за 2 попытки
### Алгоритм решения
1. Создать объект Random, для генерации чисел.
2. Создать переменную для генерации случайного числа от 0 до 9.
3. Создать цикл с предусловием, чтобы проверить хотя бы один ввод. Внутри цикла проверить ввод, увеличить счетчик попыток. Сравнить введенное число с рандомным. Если не угадали - "Вы не угадали!" и еще раз повторный ввод, если угадали - "Вы угадали!" и выход из цикла.
4. После выхода из цикла - вывести кол-во потраченных попыток.
# Задание 4
## Задача 1
### Текст задачи
Поиск первого значения. 
Дана сигнатура метода: public int findFirst (int[] arr, int x); Необходимо реализовать метод таким образом, чтобы он возвращал индекс первого вхождения числа x в массив arr. Если число не входит в массив – возвращается -1.
Пример: 
arr=[1,2,3,4,2,2,5] 
x=2 
результат: 1
### Алгоритм решения
1. Получить массив целых чисел - arr, и целое число x.
2. Создать цикл от 0 до длины исходного массива. 
3. Сравнить текущий элемент с числом x. Если равны, то вернуть индекс этого элемента. Иначе вернуть значение -1.
## Задача 3
### Текст задачи
Поиск максимального. 
Дана сигнатура метода: public int maxAbs (int[] arr); Необходимо реализовать метод таким образом, чтобы он возвращал наибольшее по модулю (то есть без учета знака) значение массива arr. 
Пример: 
arr=[1,-2,-7,4,2,2,5] 
результат: -7
### Алгоритм решения
1. Получить массив целых чисел - arr.
2. Создать переменную max (максимум), присвоить ей первый элемент массива. 
3. Создать цикл от 0 до длины массива. Внутри цикла сравнить по модулю текущий элемент и максимальное число, если эл-т по модулю больше, то поменять значение max. 
4. Вернуть значение переменной max.
## Задача 6
### Текст задачи
Реверс. 
Дана сигнатура метода: public void reverse (int[] arr); Необходимо реализовать метод таким образом, чтобы он изменял массив arr. После проведенных изменений массив должен быть записан задом-наперед. 
Пример: 
arr=[1,2,3,4,5] 
результат: arr=[5,4,3,2,1]
### Алгоритм решения
1. Получить массив с целыми числами.
2. Создать цикл идущий с 0 до половины длины массива. 
3. Внутри цикла создать переменную copy и присвоить ей текущий элемент. 
4. Текущему элементу присвоить значение элемента с другого конца. 
5. И присвоить элементу с другого конца значение переменной copy. (Метод обмена через доп. переменную)

## Задача 7
### Текст задачи
Возвратный реверс. 
Дана сигнатура метода: public int[] reverseBack (int[] arr); Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, в котором значения массива arr записаны задом наперед. 
Пример: 
arr=[1,2,3,4,5] 
результат: [5,4,3,2,1]
### Алгоритм решения
1. Получить массив целых чисел.
2. Создать новый массив по длине исходного. 
3. Создать цикл от 0 до длины изначального массива.
4. Внутри цикла присвоить элементам нового массива (с начала) значения с противоположного конца исходного массива.
5. Вернуть новый массив.
## Задача 8
### Текст задачи
Объединение.
Дана сигнатура метода: public int[] concat (int[] arr1,int[] arr2); Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, в котором сначала идут элементы первого массива (arr1), а затем второго (arr2). 
Пример: 
arr1=[1,2,3] 
arr2=[7,8,9] 
результат: [1,2,3,7,8,9]
### Алгоритм решения
1. Получить два массива целых чисел - arr1, arr2.
2. Создать новый массив - newArr, по длине сумм первого массива и второго.
3. Создать цикл от 0 до длины первого массива. Внутри цикла записать элементы первого массива в новый.
4. Создать второй цикл от 0 до длины второго массива. Внутри цикла записать элементы нового массива, с индекса длины arr1, значения из массива arr2.
5. Вернуть новый массив newArr.
